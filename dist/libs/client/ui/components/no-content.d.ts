export declare const NoContentPage: ({ subText, showBackgroundColor, isHeight, showButton, isSpacing, headingText, }: {
    showBackgroundColor?: boolean;
    isHeight?: boolean;
    showButton?: boolean;
    isSpacing?: boolean;
    headingText?: string;
    subText: string;
}) => import("react/jsx-runtime").JSX.Element;
